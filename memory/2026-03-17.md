# 2026-03-17

- Built `second-brain` into a working local-first Next.js review system in `/home/davesalter/.openclaw/workspace/second-brain` with real workspace memory ingestion, workspace document surfacing, Dropbox indexing via `rclone`, task management, search, weekly review, calendar integration through `gog`, review-to-task promotion, task editing, filtering, saved views, and natural-language task capture.
- `second-brain` now stores tasks in `/home/davesalter/.openclaw/workspace/second-brain/data/tasks.json` and supports natural-language task parsing through `/api/tasks/capture`.
- Built `mission-control` as a separate local-first Next.js app in `/home/davesalter/.openclaw/workspace/mission-control` for designing custom tools.
- `mission-control` now supports tool ideas, modules, workflows, workflow composition from modules, tool blueprints, build-spec export, copy-to-clipboard export helpers, and starter implementation handoff content.
- `mission-control` is configured to run on port `3001` by default so `second-brain` can stay on port `3000`.
- Confirmed Dropbox access on the Pi is available through an `rclone` remote named `Dropbox:` and used `Dropbox:Private - Personal` as the indexed document source for `second-brain`.
- Confirmed Google Calendar and Gmail access currently work through `drs@drs-engineering.net` via `gog`; `drs7890@gmail.com` auth remained problematic/unauthorized.
- Dave set a durable Dropbox rule: Guy may read Dropbox, but must not modify anything in Dropbox without Dave's explicit permission.
- Dave renamed the assistant from Willy to Guy and wants that used consistently going forward.
