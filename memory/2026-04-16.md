# 2026-04-16

- PlanHubGuy workflow bug found and corrected: runner had been relying on `Unique Emails`, `Unique Emails 2`, and `Unique Emails 3` as primary candidate sources instead of rebuilding unique-email candidates directly from `PlanHub Leads` each run. Updated workflow now rebuilds unique emails from `PlanHub Leads`, writes refreshed unique-email tabs back to the workbook, then applies excavation-confidence filtering and outreach from that fresh pipeline.
- Verified user complaint was substantially correct: three internal DRS test addresses (`Luke@DRS-Engineering.net`, `Olivier@DRS-Engineering.net`, `Jody@DRS-Engineering.net`) were present in `PlanHub Leads` but missing from the Unique Emails tabs, so they were skipped by the old process.
- Tightened PlanHubGuy greeting handling so missing first-name data no longer produces awkward bare `Hello` greetings.
- Found and fixed a separate PlanHubGuy state-management bug: `/mission-control/app/api/planhubguy/route.ts` was flattening the saved state to a reduced shape before writing it back, which could clobber flags and leave the UI/state inconsistent.
- Restored PlanHubGuy state after that bug: set it back to enabled/live, corrected stale process-stage display, and made the Run now control show immediate in-progress stage feedback.
- Dave later manually stopped PlanHubGuy.
