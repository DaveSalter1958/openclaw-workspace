- PlanHubGuy status as of 2026-04-26: repaired runner/write-path bugs and cleaned workbook/log corruption enough for live-readiness assessment.
- Confirmed fixes included: initial Outreach Log row write shape corrected, inbound reply/bounce Outreach Log column mapping corrected, Response Log cleanup/dedupe improved, internal-only explicit sample path no longer blocked by global enabled guard.
- Current PlanHubGuy state before any new live test: `/home/davesalter/.openclaw/workspace/memory/planhubguy-state.json` shows `enabled: false`, `mode: live`, `confidenceLevels: ["High", "Medium"]`, and live-ready/paused status.
- For a safe Medium-only live test, recommended config is: `enabled: true`, `mode: live`, `confidenceLevels: ["Medium"]`, plus runtime flags `PLANHUBGUY_DISABLE_FOLLOWUPS=1` and `PLANHUBGUY_MANUAL=1`.
- Important behavioural finding: initial outreach respects `confidenceLevels`, but follow-ups are driven by Outreach Log state unless explicitly disabled, so Medium-only testing should use the follow-up disable guard.
- Last verified readiness summary given to Dave: logs/sheets appeared clean, with 0 reported Outreach Log problems, 0 reported Response Log problems, and 0 follow-up-eligible rows at that check; runtime config still needed adjustment before actually running the Medium-only live test.
- Memory search remains broken due to embeddings/provider auth failure with bad OpenAI API key (`401`, incorrect key `11111`), so verification relied on direct file/code inspection rather than memory search.

- PlanHubGuy live Medium-only manual run was executed with `PLANHUBGUY_DISABLE_FOLLOWUPS=1`, `PLANHUBGUY_MANUAL=1`, and `PLANHUBGUY_SKIP_INBOUND_MONITOR=1` after patching the inbound-monitor stall path.
- That run completed with state detail `Run complete. Initial emails: 9, follow-ups skipped`, meaning 9 real live initial outreach emails were sent and no follow-ups were sent.
- Confirmed recipients from the run log: `carlingle@lingledesign.com`, `carllingle@lingledesign.com`, `cory.niederhaus@qdoba.com`, `dan@aegltd.com`, `dmann@waremalcomb.com`, `jamal.farha@qdoba.com`, `kyle.brown@qdoba.com`, `kylebennehoff@ingledesign.com`, `nparrish@cushnet.net`.
- Dave reported the 9 new Outreach Log entries were written into the wrong columns, shifted to `H:S` instead of `A:L`.
- Root cause identified: although the append target range was `Outreach Log!A:L`, the runner still had a bad outreach row normalization/mapping path, causing live-send appended rows to be shifted right by 7 columns.
- I reported that `planhubguy-runner.py` was patched so future Outreach rows map correctly into `A:L`.
- Important unresolved item at compaction time: after attempting sheet repair, I had not yet fully re-verified the 9 repaired rows. I believed rows `1252:1260` were restored to start in `A:E`, but I also noted a caveat that a cleanup pass may have blanked the shifted `H:S` block and that one more surgical verification/repair was still needed for the remaining expected `F:L` fields on those 9 rows.
