- Continued PlanHubGuy hardening in `mission-control/scripts/planhubguy-runner.py`: persisted send-time linkage hints in Outreach `notes` as `linkage:subject=...;email=...`, and updated inbound matching to prefer parsed linkage-note evidence before weaker heuristics.
- Audited `Response Log` cleanup risk and concluded active outbound suppression does not appear to depend on `Response Log`; current use is mainly duplicate suppression during response append.
- Created a new safer cleanup script at `mission-control/scripts/planhubguy-response-cleanup-safe.py` for `Response Log` dedupe with dry-run summary output.
- Dry run of safe response cleanup reported `231` original rows, `182` cleaned rows, `49` duplicate drops, with no more aggressive drop reasons.
- Applied `Response Log` cleanup live, reducing rows from `231` to `182` by removing duplicates.
- Important caveat: the intended Google Sheets archive tab `Response Log Raw Archive` was not created; the archive-first guard failed because the simple sheet update path does not create a new tab. Cleanup still applied, so future archival tooling should hard-fail before mutation unless archive creation succeeds.

- Patched `mission-control/scripts/planhubguy-runner.py` so targeted inbound recovery can execute before the global disabled guard and while PlanHubGuy remains paused, reusing precomputed inbound-recovery flags.
- Refactored inbound audit/recovery helpers into shared library `mission-control/scripts/planhubguy_inbound_audit_lib.py` covering Gmail search, sender extraction, classification, sender+subject lookup, sheet reads/appends, and response-log key building.
- Refactored `mission-control/scripts/planhubguy-mailbox-valid-reply-audit.py`, `mission-control/scripts/planhubguy-response-gap-audit.py`, and `mission-control/scripts/planhubguy-response-backfill-missing.py` to use the shared inbound audit library.
- Added `mission-control/scripts/PLANHUBGUY-INBOUND-RECOVERY-README.md` documenting the best current repair method: keep PlanHubGuy disabled, run inbound-only with `PLANHUBGUY_IGNORE_SEEN_INBOUND=1`, and prefer targeted entries in `sender=... | subject=...` format over thread-only or Gmail-id assumptions.
- Completed a fresh full PlanHubGuy inbound re-audit on 2026-04-28 morning. Final audited state reached mailbox external valid replies `82`, `Response Log` valid replies `76`, exact thread coverage `82/82`, semantic coverage `82/82`, and `missingCount: 0`, meaning the current audited external reply set is fully represented in `Response Log`.
- Reorganized `Response Log` again after backfill so valid replies remain surfaced first while preserving bounce/system history.
- Saved the mission-control working tree into git with commit `7acac1f` and message `Harden PlanHubGuy inbound recovery and ops tooling`.
- Updated repo hygiene to ignore transient artifacts via `.gitignore` entries for `scripts/tmp/` and `*.bak`.

- Command prefixes Dave wants interpreted directly:
  - `task ...` means create a task.
  - `my life ...` means record the information in the MyLife folders.
  - `calendar ...` means add an event to Dave's calendar.
