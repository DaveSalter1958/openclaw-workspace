# 2026-04-30

- Dave shorthand preference: `p` means `proceed`.
- Investigated PlanHubGuy bounce issue: root cause was some live outbound messages leaking into the legacy `drs@drs-engineering.net` sent mailbox, causing real bounce/delay notices to return to DRS.
- Patched `mission-control/scripts/planhubguy-runner.py` to fail closed if live sends authenticate as the wrong mailbox or if a sent message appears in the legacy DRS sent mailbox.
- Cleaned stale PlanHubGuy UI wording in `mission-control/app/components/PlanHubGuyPanel.tsx` so test/output language points to Dave inbox rather than DRS inbox.
- Added Mission Control inbound-review/reply-queue tooling for PlanHubGuy: new API routes plus `mission-control/scripts/planhubguy-review.py` now let the UI run inbox+spam classification, open follow-up/automatic-reply queues, view thread content, generate editable suggested replies, and send threaded replies directly from the email viewer.
- Improved the new PlanHubGuy email viewer so it resolves Gmail custom label IDs back to human label names and now supports manual classification buttons (`Mark follow up` / `Mark automatic`) directly on the open email.
- Dave uploaded the DRS Statement of Qualifications PDF; copied it to `mission-control/assets/DRS-Statement-of-Qualifications.pdf` and verified PlanHubGuy reply drafts now detect it and mark `attachmentAvailable: true`.
- Diagnosed an internal mail-routing quirk: `Dave@drs-engineering.net -> DRS@drs-engineering.net` messages were not landing, while `DRS -> DRS` self-delivery worked immediately. Patched `planhubguy-runner.py` so DRS-targeted internal sends automatically reroute through the DRS mailbox identity instead of Dave, then verified delivery with subject `PlanHubGuy Delivery Fix Test` appearing in DRS inbox/sent.
- Dave does not want Telegram to show low-level working/tool/command chatter; only send high-level progress summaries when needed.
- Fixed PlanHubGuy signature rendering again: instead of relying on Gmail signature HTML with an external `googleusercontent` image URL, `planhubguy-runner.py` now embeds the local signature image file directly as a data URI after the body text, matching Dave's preference for image-only signature rendering.
# 2026-04-30

- Dave shorthand preference: `p` means `proceed`.
- Investigated PlanHubGuy bounce issue: root cause was some live outbound messages leaking into the legacy `drs@drs-engineering.net` sent mailbox, causing real bounce/delay notices to return to DRS.
- Patched `mission-control/scripts/planhubguy-runner.py` to fail closed if live sends authenticate as the wrong mailbox or if a sent message appears in the legacy DRS sent mailbox.
- Cleaned stale PlanHubGuy UI wording in `mission-control/app/components/PlanHubGuyPanel.tsx` so test/output language points to Dave inbox rather than DRS inbox.
- Added Mission Control inbound-review/reply-queue tooling for PlanHubGuy: new API routes plus `mission-control/scripts/planhubguy-review.py` now let the UI run inbox+spam classification, open follow-up/automatic-reply queues, view thread content, generate editable suggested replies, and send threaded replies directly from the email viewer.
- Improved the new PlanHubGuy email viewer so it resolves Gmail custom label IDs back to human label names and now supports manual classification buttons (`Mark follow up` / `Mark automatic`) directly on the open email.
- Dave uploaded the DRS Statement of Qualifications PDF; copied it to `mission-control/assets/DRS-Statement-of-Qualifications.pdf` and verified PlanHubGuy reply drafts now detect it and mark `attachmentAvailable: true`.
- Diagnosed an internal mail-routing quirk: `Dave@drs-engineering.net -> DRS@drs-engineering.net` messages were not landing, while `DRS -> DRS` self-delivery worked immediately. Patched `planhubguy-runner.py` so DRS-targeted internal sends automatically reroute through the DRS mailbox identity instead of Dave, then verified delivery with subject `PlanHubGuy Delivery Fix Test` appearing in DRS inbox/sent.
- Dave does not want Telegram to show low-level working/tool/command chatter; only send high-level progress summaries when needed.
- Fixed PlanHubGuy signature rendering again: instead of relying on Gmail signature HTML with an external `googleusercontent` image URL, `planhubguy-runner.py` now embeds the local signature image file directly as a data URI after the body text, matching Dave's preference for image-only signature rendering.
- Mission Control runs under the Next.js base path `/mission-control`; direct links should include that prefix (for example `/mission-control/workflows`).
- The PlanHubGuy "Review inbox + spam now" action had two real bugs: it skipped already-seen inbound messages and its Gmail search patterns were too narrow for subjects like `Re: [External] Regarding ...`. Patched inbound review to force relabel/reprocess for manual review passes and broadened the search patterns; verified previously unlabeled replies from Craig Stradley and Karen Valine were labeled `Follow up`.
- The PlanHubGuy reply viewer had several UI-state bugs that were fixed: auto-load suggested replies for follow-up emails, keep the reply editor visible, load SOQ availability when opening the thread instead of only after draft generation, and make the send action a clearly visible large button under the editor.
- When Mission Control code changes add new API routes or front-end behavior, the running Next.js server may need a restart before the browser stops throwing client-side exceptions against stale route state.
# 2026-04-30

- Dave wants the PlanHubGuy follow-up workflow UI tightened further: a smaller follow-up queue showing mainly a numbered list of email addresses, and a much larger email viewer.
- Dave wants the follow-up workflow to auto-open email #1, show only the contact's response body in the viewer, automatically load the suggested reply below it, allow inline editing, and provide a send button that relabels the message from `Follow up` to `Responded` so it disappears from the follow-up queue.
- Dave wrote the song "Life Trails."
- Suno set Dave's song "Life Trails" to music.
- Dave wants the one-page "Life Trails" guitar play sheet remembered for future reference; current easy-play version is in key `Am`, 4/4, with main verse progression `Am - G - C - F`, chorus `C - G - Am - F`, ending `C - G - F - Am`, and suggested strum `D D U U D U`.
- Dave does not want to see code-looking or tool-chatter progress text in Telegram while work is in progress; keep Telegram updates human, brief, and high-level only.
