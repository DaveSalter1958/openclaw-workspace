- Dave answered PlanHubGuy workflow spec question 1: Low-likelihood projects should always be eligible for selection for outreach, controlled by send settings rather than permanently excluded.
- Dave answered PlanHubGuy workflow spec question 2: out-of-office replies should create a delayed reminder/follow-up task when a return date is detected.
- Dave answered PlanHubGuy workflow spec question 3: human no-interest/decline replies should be labeled/state `Closed - Not Interested`, logged, and kept out of the manual follow-up queue unless reopened.
- Dave answered PlanHubGuy workflow spec question 4 by uploading the DRS Statement of Qualifications PDF. Stored stable copy at `mission-control/data/planhubguy/DRS_Statement_of_Qualifications.pdf` for PlanHubGuy follow-up drafts/sends.
- Dave answered PlanHubGuy workflow spec question 5 by uploading the email signature image. Stored stable copy at `mission-control/data/planhubguy/DRS_Email_Signature.jpg` for outgoing PlanHubGuy replies.
- Updated `mission-control/scripts/planhubguy-runner.py` so PlanHubGuy uses the stable uploaded signature image at `mission-control/data/planhubguy/DRS_Email_Signature.jpg`, embeds it as an inline raw MIME image with Content-ID `<drs-email-signature>`, and avoids gog simple `--body-html` for PlanHubGuy sample/test sends that need the signature.
- Fixed PlanHubGuy raw Gmail MIME structure after Dave showed Gmail rendering the signature as a broken inline image plus attachment. `gmail_send_raw` now builds multipart/related with multipart/alternative plus inline image `Content-ID: <drs-email-signature>` and no filename, so Gmail should render the DRS signature inline instead of as a visible attachment.
- Dave showed the resent invoice test still had `Best regards` above the signature and smudgy signature text. Updated PlanHubGuy email rendering to strip trailing sign-offs (`Best regards`, `Thanks`, etc.) and render the signature text as crisp HTML, using a cropped inline `DRS_Email_Logo.png` image only for the logo. Verified HTML no longer contains Best regards/Thanks and MIME has inline image/png with no attachment filename.
- Dave instructed that no external emails should be sent for now due to Gmail spam/security warning and domain authentication issues. Disabled PlanHubGuy (`enabled: false`) and set `externalEmailHold: true` in `memory/planhubguy-state.json`; added a hard guard in `planhubguy-runner.py` so external recipients are blocked while the hold is active. Verified no PlanHubGuy crontab entry was present and external-send guard blocks non-DRS recipients.
- Dave asked to optimize PlanHubGuy and audit Mission Control follow-up queue because many contacts had already been responded to. Captured queue at 127 Follow up items, audited against sent mail in `drs@drs-engineering.net` and `Dave@DRS-Engineering.net`, found 114 with sent-reply evidence, reconciled Gmail labels/Outreach Log: queue reduced to 9 unresolved items; 107 threads/261 Gmail messages relabeled Responded, 108 manual-reply-reconciled Outreach rows appended, and 144 Outreach rows marked Responded. Added `planhubguy-followup-sync.py` plus audit/reconcile helper scripts and wired Mission Control inbound-review refresh to run the sync before refreshing queue snapshots. External email hold remains active; no external emails were sent.
