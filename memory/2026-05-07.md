
- Audited PlanHubGuy Follow up queue after Lauren false-positive bug. Patched `planhubguy-runner.py` so inbound relabeling preserves/sets `Responded` when a thread has a later internal/SENT reply, and removed the `planhubguy-review.py` behavior that stripped `Responded` from mixed-label threads. Cleaned 61 false positives from the Follow up queue; 17 genuine unresolved items remained. Refreshed the Mission Control Follow up snapshot to match the live Gmail label state.

- Further refined PlanHubGuy Follow up handling: detected contact emails that are replies to Dave/PlanHubGuy's already-sent follow-up. Short acknowledgements/reactions (thanks, you're welcome, not a problem, Outlook liked-message reactions) are now treated as no-reply-needed and marked `Responded`; removed 5 such items from the queue, leaving 7 active Follow up items. Patched suggested replies so post-follow-up responses do not attach the SOQ again and instead recommend no reply for simple acknowledgements or a concise thank-you/update response for substantive replies.

- Dave clarified PlanHubGuy escalation rule: if a contact/client reply indicates they may want to discuss actual work — whether in response to a PlanHubGuy follow-up, later on the same project, or via direct email to Dave@DRS-Engineering.net for a new project — PlanHubGuy should classify it separately as `Possible Work` and email Dave at DRS@DRS-Engineering.net. Implemented `Possible Work` Gmail label, intent keyword detection, internal DRS notification with thread details, deduped notification state, queue snapshot support, and Mission Control UI queue/button support.

- Dave flagged Jim@craiglawson.com's 2026-04-22 reply as clearly out-of-office but incorrectly classified as `Follow up`. Inspected the body: it said Jim would be out of the office and return May 11, with absence language. Strengthened PlanHubGuy auto-reply detection to inspect full message body text (not just snippets/headers) and added additional OOO markers including “out of the office,” “will be out,” “return to the office,” “during my absence,” and limited-email-access language. Updated suggested-reply logic to recommend no reply for automatic/OOO text. Relabeled Jim's message from `Follow up` to `Automatic Reply`; refreshed queues and restarted Mission Control.

- Configured PlanHubGuy weekday schedule using user systemd timer `planhubguy-weekday-run.timer`: runs Mon..Fri at 13:00 local time. Service first runs inbound-only processing, then runs PlanHubGuy with `PLANHUBGUY_MANUAL=1` so the 1pm schedule is a full run despite the script's normal pre-3pm send-window guard. Mission Control remains active on port 3010; PlanHubGuy state is enabled/live with externalEmailHold false.
