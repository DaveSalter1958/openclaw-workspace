
## PlanHubGuy hardening / backup pass
- Dave asked to refactor, reauthenticate, audit logs, verify PlanHubGuy process correctness, and make sure it is backed up after many patches.
- Completed a hardening pass on `/home/davesalter/.openclaw/workspace/mission-control`.
- Added `mission-control/scripts/planhubguy-backup.py` to create timestamped local PlanHubGuy backups under `backups/planhubguy/planhubguy-state-*.tgz`, with SHA256 checksum files and 30-archive retention. Backup includes key state/log/template/learning files and core PlanHubGuy scripts/UI files.
- Created verified backup: `/home/davesalter/.openclaw/workspace/backups/planhubguy/planhubguy-state-20260514-211947Z.tgz`.
- Refactored scheduled run into `mission-control/scripts/planhubguy-cron.sh`, with timestamped steps, pre-run backup, inbound review, scheduled send run, and queue refresh.
- Replaced ambiguous scheduled `PLANHUBGUY_MANUAL=1` behavior with explicit `PLANHUBGUY_SCHEDULED_SEND=1` for scheduled sends.
- Made PlanHubGuy state writes atomic in Python runner, queue refresh script, and Next.js queue-state helper.
- Added timeout/buffer protections to reply draft/send API calls to avoid UI hangs if Gmail/gog stalls.
- Verified `gog auth list` and live Gmail access to `Dave@DRS-Engineering.net`; no interactive OAuth reauth required.
- Verified required Gmail labels exist: `Follow up`, `Possible Work`, `Responded`, `Automatic Reply`, `Bad Email`.
- Validated JSON logs/state: `memory/planhubguy-state.json`, `memory/planhubguy-log.jsonl`, `mission-control/scripts/tmp/planhubguy-reply-send.log`, and `mission-control/data/planhubguy/reply-edit-learning.jsonl`.
- Verification passed: Python compile, `npm run build`, Mission Control restart, timer enabled for weekday 1pm, `/mission-control/workflows` HTTP 200, backup archive content verified.

## DRS Dropbox Organization Playbook
- Dave provided a basic playbook for DRS Engineering Dropbox organization.
- Saved formal playbook at `DRS-DROPBOX-PLAYBOOK.md`.
- Added long-term memory rules covering folder types, project naming, filing behavior, Operations sensitivity, and strict no-modify-without-approval Dropbox rules.

## Mission Control Photos page unresponsive fix
- Dave reported Mission Control Photos page showed browser "page unresponsive" wait/cancel dialog.
- Diagnosed `/api/photos` returning 13,186 photo items and the React UI rendering all cards/thumbnails at once.
- Patched `mission-control/app/components/PhotoReviewConsole.tsx` to render photos in chunks of 200, add "Load next 200" / "Show fewer", and select only loaded photos to avoid huge browser state updates.
- Verified `npm run build` passed, restarted `mission-control.service`, `/mission-control/photos` returned HTTP 200, `/api/photos` returned HTTP 200, and live bundle contains chunked-loading UI text.

## Mission Control Photos editor action redesign
- Dave asked to redo the Photos edit window for fast selected-photo actions without a review step.
- Updated `PhotoReviewConsole.tsx` so Edit Photos is organized around immediate actions: select loaded photos/deselect, delete selected now, mark selected for marketing, and change selected work type.
- Removed the old delete-candidate/review-oriented buttons from the main editor flow.
- Updated `/api/photos/delete` to accept explicit `photoPaths` so delete selected now deletes exactly the selected Dropbox project photos and clears thumbnail cache/state/catalog entries.
- Verified `npm run build`, restarted Mission Control, `/mission-control/photos` HTTP 200, and live bundle contains the new action labels.

## Mission Control Photos range selection
- Dave asked for Ctrl-click range selection in the Photos editor: select one photo, hold Ctrl, click another non-adjacent photo, and select all photos between.
- Updated `PhotoReviewConsole.tsx` checkbox selection so Ctrl, Shift, or Cmd/Meta click selects the full range between the last selected photo and the clicked photo.
- Verified `npm run build`, restarted Mission Control, and confirmed live Photos bundle contains the new range-selection logic.

## Mission Control Photos rename-after-label fix
- Dave reported project `2009-07` photos were relabelled in Mission Control but Dropbox filenames did not change to `project number work type sequence.jpg`.
- Diagnosed cause: recent Photos editor simplification saved work-type labels but removed the visible rename step and did not automatically run the Dropbox rename endpoint.
- Updated `PhotoReviewConsole.tsx` so the work-type action is now explicitly `Change work type & rename files`; after saving state it calls `/api/photos/rename` for the selected paths.
- Updated `/api/photos/rename` naming format from underscore style to space-separated format: e.g. `2009-07 Perm Soldier Pile Wall 001.jpg`; JPEG/JPG extensions normalize to `.jpg`; common plural `Walls`/`Systems` is singularized for cleaner labels.
- Ran dry-run for project `2009-07`: 21 eligible photos, 0 skipped. No actual Dropbox rename was performed in this check pending Dave's explicit approval.

## Dropbox photo rename applied for project 2009-07
- Dave explicitly approved: “Rename the 2009-07 Dropbox photos now.”
- Backed up photo catalog/state before modifying Dropbox under `backups/photos/` with stamp `20260514-151013`.
- Initial API rename attempt hit Dropbox `too_many_requests` write-rate limiting with a 300-second retry delay and completed zero files.
- Retried with a serialized recoverable script that waited through Dropbox cooldowns and persisted local catalog/state after each successful move.
- Completed rename of all 21 project `2009-07` photos.
- Verified local catalog has 21/21 renamed paths and rclone directory checks found 0 missing renamed Dropbox files.

## Mission Control Photos queued Dropbox operations
- Dave asked to speed up photo review by making delete/rename selections across multiple projects first, then running Dropbox writes later in the background to avoid Dropbox rate-limit delays.
- Added queued Dropbox operation workflow:
  - `Change work type & queue rename` saves labels locally and marks photos with `pendingRename`.
  - `Queue selected for delete` / per-card `Queue Delete` marks delete locally instead of immediately deleting from Dropbox.
  - `Run queued Dropbox changes` starts a detached background batch via `/api/photos/apply-pending`.
  - `Refresh batch status` reads `.tmp/photo-pending-ops-status.json`.
- Added `mission-control/scripts/photos-apply-pending.py`, which backs up catalog/state, applies queued deletes and renames slowly/serially with rclone retry/cooldown settings, updates catalog/state after each successful write, and clears thumbnail cache.
- Verified Python compile, `npm run build`, Mission Control restart, apply-pending status endpoint, and live Photos bundle contains queued-operation labels.

## PlanHubGuy Playbook
- Dave asked for a PlanHubGuy process playbook.
- Created `PLANHUBGUY-PLAYBOOK.md` at the workspace root.
- Playbook covers purpose, files/locations, Google auth/accounts, systemd schedule, backups, logs/state, normal workflow, duplicate suppression, inbound labels, Mission Control reply queue, suggested reply rules, SOQ/signature assets, safety rules, common operations, health checklist, and trust ladder.

## Mission Control Photos process PDFs
- Dave asked for the same playbook and simple summary in PDFs for the photo process and to email them to him.
- Created `PHOTOS-PROCESS-PLAYBOOK.md` / `.pdf` and `PHOTOS-PROCESS-SIMPLE-SUMMARY.md` / `.pdf` at the workspace root.
- Emailed both PDFs to `DRS@DRS-Engineering.net` with subject `Mission Control Photos Process Playbook and Summary`.

## Mission Control Photos edit-panel layout fix
- Dave reported the Photos page end-of-session button overlapped the work type window.
- Updated `PhotoReviewConsole.tsx` and `app/globals.css` so the work-type controls have their own responsive grid column and the end-of-session Dropbox batch controls sit in a full-width row below the main edit actions.
- Verified `npm run build`, restarted Mission Control, and confirmed live Photos bundle contains the new layout classes.
