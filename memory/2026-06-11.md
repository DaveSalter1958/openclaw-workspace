- Personal photo add-folder processing: Dave reattached the Seagate photo drive and added `/media/davesalter/Seagate Portable Drive/Add photos`. Built `photo-drive-audit/process_add_photos_fast.py` using size + beginning/middle/end SHA-256 fingerprints to compare against `/media/davesalter/Seagate Portable Drive/Photos by Year` without full-hashing the 200GB source. Plan found 3,754 media files in Add photos: 3,736 already duplicated in the organized library, 0 duplicates within Add photos, and 18 unique files (~0.274 GB). Copied the 18 unique files, leaving Add photos untouched, into year/device folders under Photos by Year with mtimes set from capture date. Verification had 0 errors. Plan: `photo-drive-audit/add-photos-fast-plan-20260611-130117.csv`; apply manifest: `photo-drive-audit/add-photos-apply-from-plan-20260611-130848.csv`; summary: `photo-drive-audit/add-photos-apply-summary-20260611-130848.json`.
- Mission Control Tasks UI update: Dave requested much smaller task windows showing only task name, with Delete and Completed buttons on each task, plus a Reply button for email-derived tasks that sends via the receiving Gmail account. Updated `mission-control/app/page.tsx`, added client `TaskColumn`, rewrote `EmailTaskColumn` compact rows with Reply/Done/Delete, and added compact CSS. Email Reply uses existing `/api/tasks/email-reply` flow, which determines the account from task notes and sends through `gog`. `npm run build` passed, `mission-control.service` restarted, and `/mission-control` render includes `compact-task-row`.
- Follow-up Mission Control email task change: Dave clarified email tasks must track replies/responses. Patched `/api/tasks/email-reply` so sending a reply keeps the task `open` instead of marking it done. Updated `EmailTaskColumn` so email task rows are clickable and open a summary/history modal; Reply/Done/Delete buttons stop row click propagation. The modal now shows received account/from/date/subject/latest email summary plus an `Email / reply history` section built from task notes `Action history`. Sending a reply now leaves the task active in the list and shows "Task kept active." Build passed, `mission-control.service` restarted and active.
