- Mission Control email-task filtering tightened per Dave: email tasks should represent messages that genuinely require Dave to respond/decide/approve/review/confirm, not marketing/signup prompts or automated alerts. Updated `bin/email-to-notion-tasks.py` with stronger marketing/bulk filters, less generic action detection, quoted-text cleanup, and feedback-subject suppression. Added `state/email-task-feedback.jsonl` logging from Mission Control Ignore/Delete-email actions via `/api/tasks/email-action`, so Dave's UI decisions become examples for future filtering. Dry run for latest 15 DRS inbox messages found no actionable candidates after filtering; build passed and `mission-control.service` restarted.
- DRS spam automation update: reviewed latest 100 messages in `DRS@DRS-Engineering.net` Spam and extracted 71 obvious spam/marketing senders, excluding ambiguous/important senders such as Google account notices, LinkedIn, and personal club-style mail. Saved reviewed list to `state/drs-reviewed-spam-senders.json`. Gmail rejected direct filter action `addLabelIds: SPAM`, so created `scripts/drs-spam-sender-enforcer.py`, which scans DRS Inbox for those senders and batch-moves matches to Spam by adding `SPAM` and removing `INBOX`. Dry run and live run both matched 0 current Inbox messages. Added cron every 30 minutes with logs at `memory/daily-drs-spam-enforcer.log` and JSONL run log at `memory/email-cleanup/drs-spam-sender-enforcer.jsonl`.
