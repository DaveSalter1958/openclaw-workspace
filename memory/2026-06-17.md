- Mission Control Photos process removal: Dave asked to remove the Photos process and all associated code/UI from Mission Control. Removed the sidebar Photos nav item, `app/photos/*`, `app/api/photos/*`, `PhotoReviewConsole`, photo-specific CSS, photo refresh/apply scripts, stale PlanHubGuy changeset-map references, and the weekly `photos-refresh-cron.sh` crontab entry. `npm run build` passed; grep found no remaining Mission Control app/script photo references. Restarted `mission-control.service` successfully after clearing a temporary test Next process. `/mission-control/` returned 200 with no Photos nav/text, `/mission-control/photos` returned 404, and crontab had no remaining photo references.
- Mission Control Skills/ClawHub section removal: Dave asked to remove the Skills section. Removed the sidebar Skills nav item, `app/clawhub/page.tsx`, `app/api/clawhub/route.ts`, and `app/components/ClawHubBrowser.tsx`; changed the legacy `/ideas` redirect from `/clawhub` to `/`. Left shared `clawhub-*` button CSS class names in place because PlanHubGuy still uses them as styling utilities. `npm run build` passed, restarted `mission-control.service`, `/mission-control/` returned 200 with no Skills/ClawHub text, and `/mission-control/clawhub` plus `/mission-control/api/clawhub` returned 404.
- Updated PlanHubGuy Marketing Statistics to today's date (2026-06-17) at Dave's request. Ran `scripts/planhubguy-marketing-stats.py`, refreshed PlanHubGuy queues, built Mission Control, and restarted `mission-control.service`. Current stats file date range is 2026-04-01 to 2026-06-17; summary: 1,330 initial emails, 658 unique initial-outreach contacts, 2,372 auto follow-ups; rows: 4,019 outreach rows in range, 1,161 response rows in range. Live `/mission-control/workflows` returned 200 and includes today's date.
- PlanHubGuy statistics enhancement: Dave asked to add Follow-up 1 sent, Final follow-up sent, and real-person response counts. Updated `mission-control/scripts/planhubguy-marketing-stats.py` to compute `followup1Emails`, `finalFollowupEmails`, and unique real-person response threads (`valid` + `possible_work`, excluding bounces/automatic/system rows). Updated `PlanHubGuyPanel.tsx` to show these in the top summary and stats table. Regenerated 2026-06-17 stats: Follow-up 1 sent 1,687; Final follow-ups sent 685; real-person response threads 330 (50.15% of 658 unique initial contacts). `npm run build` passed, `mission-control.service` restarted, and live `/mission-control/workflows` returned 200 containing the new labels and counts.
- Corrected PlanHubGuy stats after Dave caught an impossible count: dashboard showed 1,330 initial emails but 1,687 follow-up 1 emails. Root cause: `planhubguy-marketing-stats.py` was reading lifetime initial counts from the mutable live Outreach Log, where older Initial rows can later be converted to FollowUp rows. Patched the generator to use the immutable 2026-05-19 campaign snapshot as a baseline plus live rows after 2026-05-19. Corrected live stats: 2,867 initial emails, estimated 2,603 unique initial-outreach contacts, 2,372 follow-up 1 emails, 685 final follow-up emails, 3,057 auto follow-ups, 330 real-person response threads, 6,241 total outreach rows/sends basis, and 202 bad/undeliverable addresses. Rebuilt Mission Control, restarted service, and verified `/mission-control/workflows` shows the corrected counts.
- PlanHubGuy historical-statistics basis update: Dave clarified that outbound mail from legacy `DRS@DRS-Engineering.net` / `drs@drs-engineering.net` was early test/dev traffic and can be ignored for statistics. Treated `Dave@DRS-Engineering.net` Gmail Sent as the real client-outreach source of truth and excluded legacy/test/internal traffic. Added `mission-control/scripts/planhubguy-marketing-stats-from-ledger.py`, generated Mission Control stats from `data/planhubguy/campaign-events.jsonl`, and updated `planhubguy-cron.sh` so scheduled stats refresh uses the ledger-based generator instead of the mutable Outreach Log script. Current real-outreach stats: 2,402 initial emails, 2,312 unique initial-outreach contacts, 2,212 follow-up 1 emails, 1,082 final follow-up emails, 3,294 auto follow-ups, 330 unique real-person response threads. Build passed with existing broad-pattern warnings in email attachment routes, `mission-control.service` restarted, and live `/mission-control/workflows` showed the updated counts.
- Mission Control PlanHubGuy statistics layout cleanup: Dave asked to combine the top statistic boxes and detailed lines into one clean modern table. Removed the separate PlanHubGuy stats summary boxes from `PlanHubGuyPanel.tsx`, added count pills and primary-row styling in `app/globals.css`, and added `Total auto follow-ups sent` to the ledger-based marketing stats generator so no summary value was lost. Regenerated stats, built Mission Control, restarted `mission-control.service`, and verified `/mission-control/workflows` returns 200 with the new table values (2,402; 2,212; 1,082; 3,294; 330) and no `planhub-stats-summary` box markup. Build passed with the existing broad-pattern warnings in email attachment routes.
- Mission Control PlanHubGuy UI cleanup: Dave asked to remove the Testing mode window. Removed the Testing mode section from `PlanHubGuyPanel.tsx`, plus its now-unused test sample/test group state and preload fetch. Build passed with existing broad-pattern warnings in email attachment routes, restarted `mission-control.service`, and verified `/mission-control/workflows` returns 200 with Testing mode/Open testing tools/Random test group/Explicit sample generator absent and no remaining source refs in `PlanHubGuyPanel.tsx`.
- Mission Control PlanHubGuy statistics table styling update: Dave asked to make all statistic lines narrower, text smaller, add percentage values to every row, remove green boxed numbers, and add column lines so it looks like a table. Updated `planhubguy-marketing-stats-from-ledger.py` so all stats rows have percentages, regenerated `data/planhubguy/marketing-stats.json`, and updated `app/globals.css` table styling: smaller 0.74rem body text, tighter 6px/9px padding, vertical column borders, plain count text instead of green pills, and subdued primary-row highlighting. Build passed with existing broad-pattern warnings, `mission-control.service` restarted, and `/mission-control/workflows` returned 200 with all expected percent values present.
- Mission Control PlanHubGuy layout update: Dave asked to move the whole statistics window to the end of the page. Moved the `planhub-stats-card` section in `PlanHubGuyPanel.tsx` from the top to the bottom after Logs. Build passed with existing broad-pattern warnings, restarted `mission-control.service`, and verified `/mission-control/workflows` returned 200 with PlanHubGuy email campaign appearing after Status, Process stage, Excavation confidence filter, Templates, and Logs.
- Mission Control Photos process removal: Dave asked to remove the Photos process and all associated code/UI from Mission Control. Removed the sidebar Photos nav item, `app/photos/*`, `app/api/photos/*`, `PhotoReviewConsole`, photo-specific CSS, photo refresh/apply scripts, stale PlanHubGuy changeset-map references, and the weekly `photos-refresh-cron.sh` crontab entry. `npm run build` passed; grep found no remaining Mission Control app/script photo references. Restarted `mission-control.service` successfully after clearing a temporary test Next process. `/mission-control/` returned 200 with no Photos nav/text, `/mission-control/photos` returned 404, and crontab had no remaining photo references.
- Mission Control Skills/ClawHub section removal: Dave asked to remove the Skills section. Removed the sidebar Skills nav item, `app/clawhub/page.tsx`, `app/api/clawhub/route.ts`, and `app/components/ClawHubBrowser.tsx`; changed the legacy `/ideas` redirect from `/clawhub` to `/`. Left shared `clawhub-*` button CSS class names in place because PlanHubGuy still uses them as styling utilities. `npm run build` passed, restarted `mission-control.service`, `/mission-control/` returned 200 with no Skills/ClawHub text, and `/mission-control/clawhub` plus `/mission-control/api/clawhub` returned 404.
- Updated PlanHubGuy Marketing Statistics to today's date (2026-06-17) at Dave's request. Ran `scripts/planhubguy-marketing-stats.py`, refreshed PlanHubGuy queues, built Mission Control, and restarted `mission-control.service`. Current stats file date range is 2026-04-01 to 2026-06-17; summary: 1,330 initial emails, 658 unique initial-outreach contacts, 2,372 auto follow-ups; rows: 4,019 outreach rows in range, 1,161 response rows in range. Live `/mission-control/workflows` returned 200 and includes today's date.
- PlanHubGuy statistics enhancement: Dave asked to add Follow-up 1 sent, Final follow-up sent, and real-person response counts. Updated `mission-control/scripts/planhubguy-marketing-stats.py` to compute `followup1Emails`, `finalFollowupEmails`, and unique real-person response threads (`valid` + `possible_work`, excluding bounces/automatic/system rows). Updated `PlanHubGuyPanel.tsx` to show these in the top summary and stats table. Regenerated 2026-06-17 stats: Follow-up 1 sent 1,687; Final follow-ups sent 685; real-person response threads 330 (50.15% of 658 unique initial contacts). `npm run build` passed, `mission-control.service` restarted, and live `/mission-control/workflows` returned 200 containing the new labels and counts.
- Corrected PlanHubGuy stats after Dave caught an impossible count: dashboard showed 1,330 initial emails but 1,687 follow-up 1 emails. Root cause: `planhubguy-marketing-stats.py` was reading lifetime initial counts from the mutable live Outreach Log, where older Initial rows can later be converted to FollowUp rows. Patched the generator to use the immutable 2026-05-19 campaign snapshot as a baseline plus live rows after 2026-05-19. Corrected live stats: 2,867 initial emails, estimated 2,603 unique initial-outreach contacts, 2,372 follow-up 1 emails, 685 final follow-up emails, 3,057 auto follow-ups, 330 real-person response threads, 6,241 total outreach rows/sends basis, and 202 bad/undeliverable addresses. Rebuilt Mission Control, restarted service, and verified `/mission-control/workflows` shows the corrected counts.
- PlanHubGuy historical-statistics basis update: Dave clarified that outbound mail from legacy `DRS@DRS-Engineering.net` / `drs@drs-engineering.net` was early test/dev traffic and can be ignored for statistics. Treated `Dave@DRS-Engineering.net` Gmail Sent as the real client-outreach source of truth and excluded legacy/test/internal traffic. Added `mission-control/scripts/planhubguy-marketing-stats-from-ledger.py`, generated Mission Control stats from `data/planhubguy/campaign-events.jsonl`, and updated `planhubguy-cron.sh` so scheduled stats refresh uses the ledger-based generator instead of the mutable Outreach Log script. Current real-outreach stats: 2,402 initial emails, 2,312 unique initial-outreach contacts, 2,212 follow-up 1 emails, 1,082 final follow-up emails, 3,294 auto follow-ups, 330 unique real-person response threads. Build passed with existing broad-pattern warnings in email attachment routes, `mission-control.service` restarted, and live `/mission-control/workflows` showed the updated counts.
- Mission Control PlanHubGuy statistics layout cleanup: Dave asked to combine the top statistic boxes and detailed lines into one clean modern table. Removed the separate PlanHubGuy stats summary boxes from `PlanHubGuyPanel.tsx`, added count pills and primary-row styling in `app/globals.css`, and added `Total auto follow-ups sent` to the ledger-based marketing stats generator so no summary value was lost. Regenerated stats, built Mission Control, restarted `mission-control.service`, and verified `/mission-control/workflows` returns 200 with the new table values (2,402; 2,212; 1,082; 3,294; 330) and no `planhub-stats-summary` box markup. Build passed with the existing broad-pattern warnings in email attachment routes.
- Mission Control PlanHubGuy UI cleanup: Dave asked to remove the Testing mode window. Removed the Testing mode section from `PlanHubGuyPanel.tsx`, plus its now-unused test sample/test group state and preload fetch. Build passed with existing broad-pattern warnings in email attachment routes, restarted `mission-control.service`, and verified `/mission-control/workflows` returns 200 with Testing mode/Open testing tools/Random test group/Explicit sample generator absent and no remaining source refs in `PlanHubGuyPanel.tsx`.
- Mission Control PlanHubGuy statistics table styling update: Dave asked to make all statistic lines narrower, text smaller, add percentage values to every row, remove green boxed numbers, and add column lines so it looks like a table. Updated `planhubguy-marketing-stats-from-ledger.py` so all stats rows have percentages, regenerated `data/planhubguy/marketing-stats.json`, and updated `app/globals.css` table styling: smaller 0.74rem body text, tighter 6px/9px padding, vertical column borders, plain count text instead of green pills, and subdued primary-row highlighting. Build passed with existing broad-pattern warnings, `mission-control.service` restarted, and `/mission-control/workflows` returned 200 with all expected percent values present.
- Mission Control PlanHubGuy layout update: Dave asked to move the whole statistics window to the end of the page. Moved the `planhub-stats-card` section in `PlanHubGuyPanel.tsx` from the top to the bottom after Logs. Build passed with existing broad-pattern warnings, restarted `mission-control.service`, and verified `/mission-control/workflows` returned 200 with PlanHubGuy email campaign appearing after Status, Process stage, Excavation confidence filter, Templates, and Logs.
- Mission Control PlanHubGuy UI cleanup: Dave asked to remove the `Current remit` window from the PlanHubGuy page. Removed the aside/card titled `Current remit` and its remit bullet list from `PlanHubGuyPanel.tsx`. Build passed, `mission-control.service` restarted, `/mission-control/workflows` returned 200, and the `Current remit` text was absent from the live page.
- Mission Control availability incident during PlanHubGuy ledger work: Dave reported `http://192.168.1.119:3010/mission-control` was unreachable. Found `mission-control.service` stopped/inactive and a stray `next-server` process holding port 3010. Killed the stray process and restarted `mission-control.service`; verified service active, listening on `0.0.0.0:3010`, and local `/mission-control/` returned 200.
