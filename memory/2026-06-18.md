# Memory — 2026-06-18

- Dave repeated the request "task kids calendar friday 5pm" after the prior Google Calendar attempt failed. Confirmed Mission Control already has the task "Kids tree house and visit schedule" due Friday, 2026-06-19 at 5:00 PM, and OpenClaw has an enabled Telegram reminder for 2026-06-19 5:00 PM Pacific. Retried `gog calendar create` for `drs@drs-engineering.net`; it timed out/hung once, and `gog calendar calendars -a drs@drs-engineering.net --json --no-input` failed with `Cannot get secret of a locked object`. Google Calendar event still needs token/keyring auth fixed before it can be created.
- Mission Control Calendar section fix: Dave reported the Calendar section was not working. Root cause: `getCalendarView()` ran `gog calendar events` during server render without a timeout, so locked Google/keyring auth could hang the page and leave stuck `gog` child processes. Patched `mission-control/lib/data.ts` to kill the calendar fetch after 3 seconds and return a `googleError`, patched `app/tools/page.tsx` to show the error, added `.calendar-status` styling, rebuilt Mission Control, restarted `mission-control.service`, and verified `/mission-control/tools` returns 200 in about 3 seconds with no lingering `gog calendar events` processes. Google Calendar data itself remains unavailable until the local keyring/token issue is fixed.
- Fixed the `Gogcli` keyring password problem after Dave reported the Pi was prompting for an unknown password. Switched `gog` from GNOME/libsecret keyring to the file keyring backend with `gog auth keyring file`, reauthorized `drs@drs-engineering.net` for Calendar using the remote OAuth redirect Dave provided, patched Mission Control calendar child processes to pass `GOG_KEYRING_PASSWORD` as an empty local keyring password, and added `~/.config/environment.d/50-gog-keyring.conf` plus current user-systemd environment so background services can read the file-backed token. Created the previously blocked Google Calendar event "Kids tree house and visit schedule" on Friday, 2026-06-19 from 5:00 PM to 5:30 PM with a 30-minute popup reminder. Verified `gog calendar events` returns the event and live `/mission-control/tools` returns 200 in about 1.2 seconds with real Google Calendar entries displayed.
