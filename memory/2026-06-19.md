# Memory — 2026-06-19

- Dave reported the morning brief still failed with `gog`/file-keyring error: `no TTY available for keyring file backend password prompt; set GOG_KEYRING_PASSWORD`. Root cause: the previous fix set `GOG_KEYRING_PASSWORD` for Mission Control and user-systemd, but cron jobs do not inherit that environment. Patched `second-brain/scripts/send-morning-brief.mjs` to pass `GOG_KEYRING_PASSWORD` to child processes, patched `bin/daily-drs-email-tasks.sh` and `bin/email-to-notion-tasks.py` for the same issue, and patched `scripts/drs-marketing-junk-cleanup.py` plus `second-brain/lib/data.ts` so non-interactive `gog` calls do not prompt. Verified Calendar and Gmail `gog` commands succeed from a stripped cron-like environment with `GOG_KEYRING_PASSWORD=`. `npm run build` in `second-brain` still fails on an unrelated existing `/tasks` prerender `localeCompare` error in `TaskPanel`.

- Dave is looking to purchase natural dimensional grey-tone slate flagstone: 12" x 24", at least 1" thick, with a matching bullnose option. Search result summary from 2026-06-19: exact true-slate grey 12x24x1+ bullnose options were hard to find; best practical match was Select Stone Supply “Slate Grey” in 12x24x1 with bullnosed coping available, but it is listed as sandstone rather than geologic slate. Other near matches: Stone Universe/SUI “Flagstone Slate Grey (West)” natural grey stone in 1" +/- and 2" +/- but also sandstone and not true bullnose; Stone & Tile Shoppe California Gold natural cleft slate coping in 12x24x1.5 or 12x24x2 with bullnose but rustic gold/grey rather than clean grey; Montauk Blue/Black slate coping in 12x24x3cm/1.25" is literal grey/charcoal slate but usually eased-edge rather than bullnose.
