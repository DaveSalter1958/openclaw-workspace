# 2026-07-03 Memory

## Morning Brief Gog Keyring Repair

- Dave asked why the morning brief failed. Log showed the 7:00 AM run failed at `calendar events` for `drs@drs-engineering.net` with `aes.KeyUnwrap(): integrity check failed`, meaning the local file-keyring token could not be decrypted in cron/non-interactive mode.
- Repaired by moving the bad active Gog keyring aside to `/home/davesalter/.config/gogcli/keyring.broken-20260703-090556` and restoring the last known-good keyring from `/home/davesalter/.config/gogcli.backup-20260702-162228/keyring`. The restored active token files have June 18 timestamps; the broken July 2 token files remain preserved for inspection.
- Verified from a stripped cron-like environment with `GOG_KEYRING_PASSWORD=` that `gog auth list`, `gog calendar events`, `gog gmail search`, and `gog gmail get` authenticate again. Crontab still includes `GOG_KEYRING_PASSWORD=` and the morning brief schedule remains daily at 7:00 AM Pacific. Did not manually send a replacement morning brief.

## Mission Control Access Repair

- Dave reported he could not access Mission Control. The app was running, but the active user systemd service was bound to `127.0.0.1:3010`, so it was only reachable from the Pi itself. Restored the service binding to `0.0.0.0:3010`, matching the checked-in Mission Control service file, reloaded user systemd, and restarted `mission-control.service`.
- Verified `/mission-control` returns `200 OK` over localhost, LAN `192.168.1.119:3010`, and Tailscale `100.124.204.83:3010`.

## OpenClaw Dashboard Keyring Prompt Repair

- Dave reported that running `openclaw dashboard` on the Pi opened a GNOME prompt saying an application wanted access to the locked "Default Keyring." Root cause appears to be the browser launch path: OpenClaw was healthy, but the system default browser was plain Chromium, which can ask GNOME Keyring for browser secret storage.
- Added a user-level Chromium desktop handler at `~/.local/share/applications/chromium-no-keyring.desktop` with `--password-store=basic`, set it as the default handler for `http`, `https`, and `text/html`, and closed the stale Chromium dashboard process that had been launched without the flag. Verified OpenClaw control UI returns `200 OK` at `http://127.0.0.1:18789/` and `openclaw status` reports the gateway running. Dave confirmed `openclaw dashboard` worked afterward without the keyring prompt.
