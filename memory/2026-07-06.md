# 2026-07-06

## Mission Control / PlanHubGuy / gog auth

- Mission Control access issue: Dave could not reach `http://100.124.204.83:3010/mission-control` because Mission Control had been rebound to `127.0.0.1:3010` during a security pass. Fix applied: Caddy listener bound only to Tailscale IP `100.124.204.83:3010`, proxying `/mission-control` to `127.0.0.1:3010`; LAN `192.168.1.119:3010` remained closed. Backup noted at `/home/davesalter/.openclaw/workspace/state/Caddyfile.backup-20260706-0945`.
- PlanHubGuy last known successful scheduled run before fixes was Thu Jul 2 2026 1:10 PM PDT. Fri Jul 3 1 PM run failed due `gog auth tokens export Dave@DRS-Engineering.net` exit status 4 / missing Gmail refresh token.
- Dave re-authed `Dave@DRS-Engineering.net` for `gog` Gmail. Manual PlanHubGuy run on Jul 6 succeeded after multiple restarts and overrides, completing at 12:35:41 PM PDT. It processed inbound backlog, updated labels/response logs, refreshed Mission Control queues/stats, sent 18 initial outreach emails total that day and 25 final follow-ups. Duplicate suppression worked during reruns.
- Important PlanHubGuy correction: Dave clarified `Dave@DRS-Engineering.net` is only for PlanHubGuy emails, not general DRS calendar use. PlanHubGuy routine auth should use Dave's working auth path for outbound/inbound/Sheets as configured on Jul 6, while Mission Control Calendar should use DRS calendar auth.
- PlanHubGuy was cleanly updated so routine scheduled work defaults Gmail send/inbound, Sheets, notify, and signature-source auth to `Dave@DRS-Engineering.net`; legacy DRS sent-mail leak check is off by default and non-fatal if explicitly enabled; internal DRS reroute only occurs if a legacy DRS outbound account is explicitly configured. User systemd service explicitly sets `PLANHUBGUY_SHEETS_ACCOUNT=Dave@DRS-Engineering.net` and clears `PLANHUBGUY_LEGACY_DRS_OUTBOUND_ACCOUNT`.
- Timer safety: after manual run, the attempted 1:05 PM re-enable caused a catch-up run because the timer had `Persistent=true`. It was stopped and changed to `Persistent=false`; next run became Tuesday 1 PM. Catch-up sent 43 extra final follow-up emails between 1:00 and 1:11 before being stopped. This was explicitly acknowledged as assistant error.
- Mission Control Calendar: assistant first incorrectly changed Calendar to use `Dave@DRS-Engineering.net`; Dave corrected that he only uses the calendar under `DRS@DRS-Engineering.net`. Correct state: Mission Control Calendar should use Google account `drs@drs-engineering.net` and calendar ID `drs@drs-engineering.net`; no Mission Control env override should point Calendar to Dave's PlanHubGuy account.
- Current remaining Mission Control Calendar fix as of 2:22 PM PDT: re-auth `DRS@DRS-Engineering.net` for `gog` Calendar. Existing DRS token fails with Google OAuth `unauthorized_client`; app is aimed at the correct calendar but token is stale/invalid.
- `gogcli/keyring` is the encrypted local store for Google OAuth refresh tokens. On this Pi, services normally use the file keyring noninteractively with an empty `GOG_KEYRING_PASSWORD`; Dave should not need to know/type a passphrase. If a command prompts `Enter passphrase to unlock /home/davesalter/.config/gogcli/keyring`, cancel and rerun with the expected `GOG_KEYRING_PASSWORD` environment as appropriate.
- Pi username confirmed: `davesalter`; home directory `/home/davesalter`.
