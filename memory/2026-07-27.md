# 2026-07-27

## Barkley Heights Trip Billing / Invoice Work

- Dave asked for a travel diary/itinerary, Excel expense spreadsheet with 15% markup, and PDF receipt backup for the Barkley project / Barkley Heights trip.
- Generated and sent `Barkley_Heights_billing_packet_2026-07-27.zip`.
- Dave uploaded flight receipt `Flight_Confirmation_and_Receipt---471d33c3-ca3c-4af9-9185-8dba40feb403.pdf`; flight was added as expense `R006: Alaska round trip, $896.80`.
- After adding flight, reported totals were: expenses `$1,045.41`, 15% markup `$156.81`, billable total `$1,202.22`.
- Dave then asked for an invoice based on the engagement letter/terms; generated and sent `Barkley_Heights_invoice_2026-07-27.pdf` and `Barkley_Heights_invoice_packet_2026-07-27.zip`.
- Dave later uploaded hotel and car rental receipts and asked to update expenses and invoice.
- Hotel receipt details: Holiday Inn & Suites Bellingham by IHG, check-in Thu Jul 23 2026, check-out Fri Jul 24 2026, confirmation `5674580542`, total paid `$209.30`.
- Car rental receipt details: Payless/Avis Budget, confirmation `21886108US6`, SeaTac pickup/drop-off Jul 23-24 2026, estimated total `$307.77`.
- Updated and sent replacement artifacts: `Barkley_Heights_invoice_2026-07-27.pdf`, `Barkley_Heights_invoice_packet_2026-07-27.zip`, and `Barkley_Heights_billing_packet_2026-07-27.zip`.
- Arithmetic note from known receipts: previous expenses `$1,045.41` + hotel `$209.30` + car `$307.77` = updated expenses `$1,562.48`; 15% markup `$234.37`; expense total with markup `$1,796.85`.

Correction: using per-receipt half-up rounding, updated 15% handling is `$234.38` and updated reimbursable expense total is `$1,796.86`. Updated invoice total is `$10,984.36` with professional/travel time `$9,187.50`. The invoice packet was emailed to Dave at `DRS@DRS-Engineering.net` with subject `Barkley Heights invoice packet`.

## Mission Control Access Repair

- Dave reported he could not access the Mission Control URL. Investigation showed `mission-control.service` was active but bound to `127.0.0.1:3010`, making it unreachable from LAN/Tailscale.
- Updated `/home/davesalter/.config/systemd/user/mission-control.service` ExecStart from `--hostname 127.0.0.1 --port 3010` to `--hostname 0.0.0.0 --port 3010`, reloaded user systemd, and restarted `mission-control.service`.
- Verified Mission Control listens on `0.0.0.0:3010`; `/mission-control` and `/mission-control/workflows` returned HTTP 200 over localhost, LAN `192.168.1.119:3010`, and Tailscale `100.124.204.83:3010`.

## Barkley Heights Excel Correction

- Dave reported the emailed Barkley Heights expense workbook opened as corrupt in Excel.
- Regenerated `Barkley_Heights_expenses_with_15_percent_markup.xlsx` and `Barkley_Heights_invoice_detail_2026-07-27.xlsx` as Office-compatible XLSX files using `openpyxl`.
- Verified both with `openpyxl.load_workbook`, confirmed `file` reports Microsoft Excel 2007+, rebuilt both billing and invoice ZIP packets, and verified both archives with `unzip -t`.
- Re-emailed Dave at `DRS@DRS-Engineering.net` with subject `Corrected Barkley Heights expense workbook and packet`; Gmail message id `19fa5c77103eb75f`.
- Dave asked for the invoice itself as an Excel file. Created and sent `Barkley_Heights_invoice_2026-07-27.xlsx` in `memory/job-site-inspections/2026-07-23-barkley-heights-bellingham-wa/invoice-packet-2026-07-27/`; verified it loads with `openpyxl` and identifies as Microsoft Excel 2007+.
