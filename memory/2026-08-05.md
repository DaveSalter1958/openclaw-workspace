- Mission Control access repair: Dave reported he could not reach Mission Control. The app was running, but the installed user systemd service had drifted back to `--hostname 127.0.0.1`, so it only listened on localhost. Updated `/home/davesalter/.config/systemd/user/mission-control.service` to `--hostname 0.0.0.0`, reloaded user systemd, and restarted `mission-control.service`. Verified `/mission-control` returns `200 OK` via localhost, LAN `192.168.1.119:3010`, and Tailscale `100.124.204.83:3010`.
- Caddy repair: `/etc/caddy/Caddyfile` still contained a stale `http://100.124.204.83:3010` bind block, causing Caddy to fail when that Tailscale address was unavailable or when Mission Control owned port 3010. Backed up the Caddyfile, removed the conflicting block, formatted/validated the config, restarted/reloaded Caddy, and verified `http://100.124.204.83/mission-control` and `http://raspberrypi.tailb86a3a.ts.net/mission-control` return `200 OK`.
