- Mission Control access cleanup: Pi's DHCP LAN address changed from `10.0.0.242` to `192.168.1.119`, breaking Dave's bookmark. Verified Mission Control is served on local port 3010 behind Caddy and added explicit Caddy hostnames `raspberrypi.local` and `raspberrypi` alongside `raspberrypi.tailb86a3a.ts.net`, `100.124.204.83`, and old `10.0.0.242`. Reloaded Caddy and verified `http://raspberrypi.local/mission-control`, `http://raspberrypi/mission-control`, and `http://raspberrypi.tailb86a3a.ts.net/mission-control` return 200. Confirmed the enabled user systemd `mission-control.service` is active.
