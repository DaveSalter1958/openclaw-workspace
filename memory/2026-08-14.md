- Mission Control golf dashboard: Dave asked to proceed with adding a golf scores page. Added `/mission-control/golf`, a sidebar Golf link, server-side golf summary loading from `data/golf/rounds.json`, round/course summary types, score tiles, recent score trend, course breakdown, round ledger, and hole-by-hole scorecards. Built Mission Control successfully, restarted `mission-control.service`, and verified `http://raspberrypi.local/mission-control/golf` returns 200 with the stored River Course 90 and Ranch Course 111 rounds.
- Mission Control golf GHIN slot: Dave asked to look up and include his GHIN handicap index. No stored GHIN/index was found, and public GHIN lookup appears to require JS/login or exact member lookup details. Added `data/golf/player-profile.json`, wired it into the golf dashboard, and added GHIN index/Handicap display cards that currently show not set until Dave provides the current index, GHIN number plus lookup details, or a GHIN screenshot. Build passed, `mission-control.service` restarted, and `/mission-control/golf` returns 200 with the GHIN slot.
- Dave provided GHIN number `154113`. Inspected the official GHIN app bundle: index lookup endpoints are under `https://api2.ghin.com` and direct GHIN lookup returns `401 Invalid token`; the GHIN app uses a logged-in `ghin-user-token` bearer token. Updated `data/golf/player-profile.json` with GHIN `154113`, displayed the GHIN number on `/mission-control/golf`, and left Handicap Index unset pending Dave's current index/screenshot/login-backed lookup.
- Dave provided current Handicap Index `23.7`. Updated `data/golf/player-profile.json` to show GHIN `154113` and Handicap Index `23.7` with source "Dave provided current Handicap Index by Telegram", rebuilt Mission Control, restarted `mission-control.service`, and verified `/mission-control/golf` returns 200 with both values.
