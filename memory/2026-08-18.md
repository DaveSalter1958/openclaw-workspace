# 2026-08-18

## Tailscale / Mission Control Phone Access

- Dave was setting up Android phone access to Mission Control through Tailscale.
- Correct Tailscale device for the Mission Control Pi is `raspberrypi` / `raspberryPi`, not the separate device named `pi` on the DRS tailnet.
- Mission Control Pi details observed during troubleshooting:
  - Tailscale hostname: `raspberrypi.tailb86a3a.ts.net`
  - Tailscale IP: `100.124.204.83`
  - Working URL: `http://raspberrypi.tailb86a3a.ts.net/mission-control`
  - Backup URL: `http://100.124.204.83/mission-control`
- Android Chrome confusion: Dave initially typed addresses into Google/search; direct full `http://.../mission-control` URLs eventually worked.
- Pi web routing issue was fixed: Tailscale requests were reaching the OpenClaw gateway dashboard because Caddy was only listening locally/falling through. Caddy was updated so the Mission Control route listens correctly on Tailscale addresses. Both Tailscale hostname and IP were tested from the Pi and returned the Mission Control page.

## Mission Control Mobile Layout Work

- Dave reported Mission Control pages were too large on Android and could not zoom.
- First mobile attempt made Mission Control more phone-native: sidebar became horizontal, task columns stacked, padding/card sizes reduced, and email modal constrained.
- Dave then preferred horizontal scrolling with a wide dashboard canvas, so the layout was changed to enable wide content and sideways panning on phones.
- That wide-canvas version was bad in portrait: only worked well in landscape, some pages such as Golf still would not scroll left/right, and the Tasks screen clipped the left side.
- Latest attempted fix before compaction: outer app remains phone-width so portrait starts at the left edge; horizontal scrolling moved into the Mission Control content area; Tasks, Golf, PlanHubGuy, Calendar, Studio, and other main pages were given the same wide content surface; app was rebuilt and restarted.
- Dave may still need to verify whether the latest portrait/horizontal-scroll behavior is acceptable on the phone.

## Woods Drive Mission Control Iteration

- Dave is actively shaping the Woods Drive page inside Mission Control at `http://raspberrypi.tailb86a3a.ts.net/mission-control/woods-drive`.
- A Dropbox document picker was added to the Woods Drive document panel using read-only `rclone` access:
  - API route added at `mission-control/app/api/woods-drive/dropbox/route.ts` for browsing Dropbox folders/files.
  - Picker lets Dave drill into Dropbox folders, select files, and add them to the local Mission Control document list.
  - Dropbox itself must not be modified; the picker only records selected file paths locally.
- A Dropbox document open route was added so documents selected from Dropbox can be opened from Mission Control without creating Dropbox sharing links:
  - Each document row has an `Open` button.
  - The route streams files read-only through `rclone`.
- Dave asked for the document list to be very simple:
  - Show only document title plus `Open` and `Remove`.
  - Do not show category, status, URL/path, or notes in the document rows.
  - The Dropbox browse/selection window was restyled with a light green treatment.
- Mission Control service/proxy notes from this work:
  - Caddy proxies `/mission-control` to `127.0.0.1:3010`.
  - `mission-control.service` should own the live process and is enabled.
  - Several restarts hit stale/orphaned Next processes holding port `3010`; clearing the stale listener and restarting systemd fixed live deployment.

## Woods Drive Actions UI Preferences

- Dave is refining the Woods Drive Actions page into a compact professional form-style layout.
- Implemented/desired Actions layout:
  - Compact rows, smaller/tighter inputs and buttons.
  - No visible Notes column, though existing notes data should be preserved behind the scenes.
  - Priority colors: High = red, Medium = orange, Low = yellow.
  - No separate Remove column.
  - `Complete` prompts for confirmation; if confirmed, it removes the action and saves the updated list.
  - `Add Action` should insert a blank row at the top and clear filters/search so the new row is immediately visible.
  - `Responsible` should be a manual free-text name-entry field.
  - `Due by` should be compact and only wide enough for date-like text; it should still allow/free-display values such as `ASAP` or `Before start`.
  - `Priority` column should be narrower.
